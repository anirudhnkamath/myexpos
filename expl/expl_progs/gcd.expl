int main()
{
decl
    int a,b,g,temp;
enddecl
begin
    temp = exposcall("Read", -1, a);
    temp = exposcall("Read", -1, b);

    while (a != b) do
        if (a > b) then
            a = a - b;
        else
            b = b - a;
        endif;
    endwhile;

    g = a;
    temp = exposcall("Write", -2, g);

    return 0;
end
}
